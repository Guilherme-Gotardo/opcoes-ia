psycopg[binary]>=3.1
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
pytest>=8.0
httpx>=0.27          # TestClient da API nos testes
# Servem só à API da interface (src/api) — o pipeline diário não as importa.
fastapi>=0.115
uvicorn>=0.30
