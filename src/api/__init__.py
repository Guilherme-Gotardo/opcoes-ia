"""API HTTP de leitura para a interface web (`opcoes-ia-web`).

Ver `app.py` para os três limites desta superfície: não decide, não dispara
processos operacionais e não envia ordens.

Uso:
    python -m src.api
"""
